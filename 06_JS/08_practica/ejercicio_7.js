//Completa la función que tomando dos números como argumento devuelva el más alto.
function sum(numberOne, numberTwo) {
  // insert code
  return Math.max(numberOne, numberTwo);
}

const result = sum(8, 15);
console.log(result);